type Int = 
    | Int

type String = 
    | String

type Result a b = 
    | Ok a
    | Err b

type User a b c = {
    name: String,
    age: Int
}

use Result

let ata : Result a b -> () 
    | Ok a => User {}