mod Prelude where
    pub type Bool = 
        | True 
        | False
    
    pub type String
    pub type Int
    pub type Unit

    use Bool

use Prelude

let ata (x: Int) (z: Bool) (y: String) : Int = do
    1
    2
    ()
    