type Int = 
    | Int

type String = 
    | String

type Result a b = 
    | Ok a
    | Err b

use Result

let ata (x: Result a b) (y: Result b a) : () = do
    let a = 2
    a

let be : Int = ata (Ok Int.Int) (Ok String.String)