use Prelude
use Yal.B

pub type Result ok err =
    | Ok ok
    | Err err

pub type User data = {
    name: String,
    age: Int,
    data: data
}

let addTwo (x: Int) : Int = add x 2

let ata (b: Int) (a: Int) : Int = 
       a 
    |> addTwo 
    |> addTwo